"""crude-meta — Facebook Pages and Instagram over the Meta Graph API.

One binary covers both surfaces because Meta is one site: one app, one token
(``[meta]``), one host (``graph.facebook.com``). Instagram Business is reached
only through the same app and a linked Page, so it is a product surface of Meta
rather than a separate site. The platform stays visible as the first resource
word: ``crude-meta instagram ...`` and ``crude-meta facebook ...``.
"""
