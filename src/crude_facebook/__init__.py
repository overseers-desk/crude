"""crude-facebook — a Facebook Page client over the Graph API.

Reads and edits a Facebook Page (posts, insights, comments, page profile) through
``graph.facebook.com``, in the ``crude-facebook <resource> <verb>`` grammar.
Instagram is a separate product on Meta's own roadmap (its own login path); the
Instagram client is deferred and its Facebook-Login implementation is preserved on
the ``crude-meta-instagram`` branch.
"""
